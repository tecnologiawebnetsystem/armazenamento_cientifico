====================================================================================
  STORY-02  -  Sessão HttpOnly, identidade do usuário e logout (Back)
  AJUDA TÉCNICA / REFINAMENTO
====================================================================================
Trilha.....: Back-end
Pontuação..: 2
Depende de.: STORY-01 (token já validado)
Habilita...: STORY-04, STORY-05
Objetivo...: Transformar a identidade validada do CAV4 em sessão do SIGAC:
             extrair/normalizar identificador -> resolver usuário -> emitir
             cookie HttpOnly -> expor sessão/logout.

------------------------------------------------------------------------------------
1) ONDE MEXER NO CÓDIGO (arquivos existentes)
------------------------------------------------------------------------------------
- back-end/app/core/cav4.py
    -> Função única extract_identity(claims) -> str (lower/trim) num só ponto.
- back-end/app/modules/users/repository.py
    -> Buscar usuário por e-mail/matrícula (resolve_user).
- back-end/app/core/temporary_sessions.py + app/core/authorization.py
    -> Emissão do cookie de sessão HttpOnly. NUNCA retornar access_token.
- back-end/app/api/routes/cav4_auth.py
    Endpoints já existentes:
      GET  /api/auth/session  -> retorna sessão atual
      POST /api/auth/logout   -> encerra a sessão
    -> REFINAR: 403 quando usuário não existe (sem criar sessão).

------------------------------------------------------------------------------------
2) O QUE CRIAR
------------------------------------------------------------------------------------
- Camada "resolve user" (identidade -> registro do banco).
- Exceção UsuarioNaoCadastrado (403) em app/core/exceptions.py.

------------------------------------------------------------------------------------
3) EXEMPLO DE ASSINATURA / CONTRATO
------------------------------------------------------------------------------------
    def extract_identity(claims: dict) -> str:      # ex.: e-mail normalizado
    def resolve_user(identity: str) -> User | None: # None -> 403
    def issue_session(user: User, cav4_subject: str) -> Response  # set-cookie HttpOnly

------------------------------------------------------------------------------------
4) BANCO DE DADOS
------------------------------------------------------------------------------------
- Tabela de sessões: migrações existentes
    back-end/alembic/versions/0020_restore_auth_sessions.py
    back-end/alembic/versions/0016_add_cav4_subject_to_sessions.py
  O cav4_subject já é persistido; sem nova tabela aqui.
- Vínculo de perfil do usuário vem da STORY-07/08.

------------------------------------------------------------------------------------
5) NOMES REAIS DE ENV VARS
------------------------------------------------------------------------------------
  SESSION_COOKIE_NAME, SESSION_TTL (ou equivalente em config.py),
  COOKIE_SECURE / COOKIE_SAMESITE quando aplicável.

------------------------------------------------------------------------------------
6) COMO VALIDAR (comandos)
------------------------------------------------------------------------------------
- Simular callback com usuário existente -> Set-Cookie HttpOnly presente.
- Usuário inexistente -> 403 e ausência de cookie.
- GET /api/auth/session com cookie -> dados do usuário; sem cookie -> 401.
- POST /api/auth/logout -> sessão encerrada; próxima /session -> 401.

CHECKLIST ANTI-HARDCODE
  [ ] Identidade sempre normalizada num único ponto.
  [ ] Nenhum usuário/perfil fixo; tudo resolvido no banco.
  [ ] access_token nunca sai para o front.
====================================================================================
